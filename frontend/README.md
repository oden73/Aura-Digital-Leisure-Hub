# Aura Frontend

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Set `VITE_API_URL` in `.env.local` to the backend URL (default: `http://localhost:8080`)
3. Run the app:
   `npm run dev`
